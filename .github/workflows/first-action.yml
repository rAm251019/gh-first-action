name: first workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world
        run: echo "hello ram"
      - name: goodbye
        run: echo "goodbye"
